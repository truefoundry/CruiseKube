// Package version holds the CruiseKube binary version (set at link time via -ldflags).
package version

// Version is the release identifier served by the API (e.g. stats endpoint).
var Version = "dev"
