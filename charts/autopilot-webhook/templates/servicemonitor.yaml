{{- if .Values.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "autopilot.fullname" . }}-service-monitor
  labels:
    {{- include "autopilot.serviceMonitorLabels" . | nindent 4 }}
  {{- if .Values.serviceMonitor.additionalAnnotations }}
  annotations:
    {{- toYaml .Values.serviceMonitor.additionalAnnotations | nindent 4 }}
  {{- end }}
spec:
  jobLabel: {{ .Values.serviceMonitor.jobLabel }}
  selector:
    matchLabels:
      {{ include "autopilot.selectorLabels" . | nindent 6 }}
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
  endpoints:
  {{- toYaml .Values.serviceMonitor.endpoints | nindent 2 }}
{{- end -}}

