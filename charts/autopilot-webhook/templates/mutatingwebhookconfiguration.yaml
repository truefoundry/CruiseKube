apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ include "autopilot.fullname" . }}-webhook-config
  labels:
    {{- include "autopilot.labels" . | nindent 4 }}
webhooks:
- name: {{ printf "%s.%s.autopilot.truefoundry.com" (include "autopilot.fullname" .) .Release.Namespace }}
  clientConfig:
    service:
      name: {{ include "autopilot.webhookServiceName" . }}
      namespace: {{ .Release.Namespace }}
      path: {{ printf "/clusters/%s/webhook/mutate" .Values.mutatingWebhookConfiguration.clusterID }}
      port: {{ .Values.service.httpsPort }}
    caBundle: Cg==
  rules:
  - operations: ["CREATE"]
    apiGroups: [""]
    apiVersions: ["v1"]
    resources: ["pods"]
    scope: "*"
  admissionReviewVersions: ["v1"]
  sideEffects: None
  failurePolicy: {{ .Values.mutatingWebhookConfiguration.failurePolicy }}
  reinvocationPolicy: Never
  matchPolicy: Equivalent
  timeoutSeconds: {{ .Values.mutatingWebhookConfiguration.timeoutSeconds }}
  namespaceSelector:
    {{- include "autopilot.mutatingWebhookConfigurationNamespaceSelector" . | nindent 4 }}
  objectSelector: {}
