apiVersion: v1
kind: Service
metadata:
  name: {{ include "autopilot.webhookServiceName" . }}
  labels:
    {{- include "autopilot.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpsPort }}
      targetPort: https
      protocol: TCP
      name: https
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "autopilot.selectorLabels" . | nindent 4 }}
