apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "autopilot.fullname" . }}
  labels:
    {{- include "autopilot.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "autopilot.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "autopilot.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "autopilot.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ include "autopilot.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["./autopilot"]
          args:
            - "--execution-mode=webhook"
            - "--webhook-certs-dir={{ .Values.webhook.certsDir }}"
            - "--webhook-stats-url-host={{ .Values.webhook.statsURL.host }}"
          ports:
            - name: https
              containerPort: 8443
              protocol: TCP
            - name: metrics
              containerPort: 8081
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          volumeMounts:
            - name: webhook-certs
              mountPath: {{ .Values.webhook.certsDir }}
              readOnly: true
      volumes:
        - name: webhook-certs
          secret:
            secretName: {{ include "autopilot.tlsSecretName" . }}
