controllerMode: in-cluster
dependencies:
  local:
    kubeconfigPath: ""
    prometheusURL: "http://localhost:9090"
    insecureSkipTLSVerify: false
  inCluster:
    prometheusURL: ""
    insecureSkipTLSVerify: false
executionMode: controller
controller:
  targetNamespace: ""
  targetClusterID: ""
  tasks:
    createStats:
      enabled: true
      schedule: "15m"
      metadata:
        skipMemory: false
    applyRecommendation:
      enabled: true
      schedule: "5m"
      metadata:
        skipMemory: false
        nodeStatsURL:
          host: "http://localhost:8080"
        overridesURL:
          host: "http://localhost:8080"
    nodeLoadMonitoring:
      enabled: false
      schedule: "60s"
    fetchMetrics:
      enabled: true
      schedule: "1m"
    cleanup:
      enabled: false
      schedule: "24h"
      metadata:
        retentionDays: 7
    disruptionForce:
      enabled: true
      schedule: "5m"
metrics:
  enabled: true
  port: "8081"   
server:
  port: "8080"
  auth:
    enabled: true
    username: "admin"
    password: "admin"
webhook:
  port: "8443"
  certsDir: "/tmp/webhook-certs"
  statsURL:
    host: "http://localhost:8080"
    tfyClusterToken: "<cluster-token>"
# db:
#   type: sqlite
#   database: "stats-data/cruisekube.db"
db:
  type: postgres
  host: localhost
  port: 5432
  database: cruisekube
  username: cruisekube
  password: cruisekube
  sslmode: disable
recommendationSettings:
  newWorkloadThresholdHours: 1
  disableMemoryApplication: false
  maxConcurrentQueries: 30
  oomCooldownMinutes: 5
  optimizeGuaranteedPods: true
  # When true, workloads managed by an HPA get coordinated vertical + horizontal
  # scaling instead of being skipped: CruiseKube right-sizes the request and (in
  # Cruise mode) adjusts the HPA target utilization so the absolute per-pod
  # scale-out point is preserved and the two control loops do not fight.
  # Default: false.
  hpaResourceAwareOptimization: false
telemetry:
  enabled: false
  exporterOTLPEndpoint: ""
  exporterOTLPHeaders: ""
  serviceName: ""
  traceRatio: 0.1
sentry:
  enabled: false
  dsn: ""
  environment: ""
